# I2C Bus Scanner - project configuration

CONFIG_I2C=y

# UART console backend for printk() output.
CONFIG_UART_CONSOLE=y
CONFIG_PRINTK=y

# NOTE: no interactive input is used - the sample auto-scans I2C0 once
# at boot from a dedicated thread. See src/main.c.
